{
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "command -v python3 >/dev/null 2>&1 && python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/governance-preflight.py\" || { echo 'team-ram: python3 not found, governance preflight SKIPPED (invariants unenforced this call)' >&2; exit 0; }"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|mcp__MCP_DOCKER__|mcp__allura-brain__",
        "hooks": [
          {
            "type": "command",
            "command": "command -v python3 >/dev/null 2>&1 && python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/governance-preflight.py\" || { echo 'team-ram: python3 not found, governance preflight SKIPPED (invariants unenforced this call)' >&2; exit 0; }"
          }
        ]
      }
    ]
  }
}
