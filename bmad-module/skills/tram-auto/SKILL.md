---
name: tram-auto
description: Run bounded Team RAM BMad Auto Mode.
---

# Team RAM Auto Mode

Run the existing Team RAM `/auto` contract:

```text
Scout ContextPacket → select specialist → one reversible slice → explicit verification
→ compaction receipt → repeat or terminal state
```

Respect configured token and iteration budgets. Stop for governance, destructive,
production, dependency, schema, agent-policy, or external-message boundaries. Auto Mode
writes trace receipts only; semantic promotion and protected merges remain governed.
